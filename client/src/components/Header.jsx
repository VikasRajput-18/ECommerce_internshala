import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return <div>
    <header>
        <Link to="/">Amazone</Link>
    </header>
  </div>;
};

export default Header;
